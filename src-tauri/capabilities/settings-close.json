{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-close",
  "description": "Allow the settings window to finish its close request",
  "windows": ["settings"],
  "permissions": ["core:window:allow-destroy"]
}
