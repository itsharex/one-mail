{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "add-account", "settings", "original-message"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "process:allow-exit",
    "opener:default"
  ]
}
